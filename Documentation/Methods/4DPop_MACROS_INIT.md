<!-- Initialization entry point for the 4DPop Macros component. -->

## Description

`4DPop_MACROS_INIT` is the component's initialization entry point. It simply calls `Init` to set up the shared storage and install the component's resources.

## Example

```4d
4DPop_MACROS_INIT()
```
