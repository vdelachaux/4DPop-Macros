<!-- Opens the 4DPop Macros settings dialog box. -->

## Description

`_SETTINGS` instantiates the `cs.settings` class, which displays the component's settings dialog box. It is typically invoked from a worker so the dialog runs on the main process.

## Example

```4d
_SETTINGS()
```
